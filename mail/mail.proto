syntax = "proto3";

package mail;

import "google/api/annotations.proto";

service MailService {
  rpc SendAccountVerification(AccountVerificationRequest) returns (Empty) {}
}

message AccountVerificationRequest {
  string email = 1;
}

