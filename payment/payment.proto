syntax = "proto3";

package payment;

service PaymentService {
  rpc PaymentIntentWebhook(PaymentIntent) returns (Empty) {
    option http = {
      post: "/api/v1/payment/stripe/webhook/paymentintent",
      body: "*"
    };
  }
}

message PaymentIntent {
  bytes body = 1;
}

message Empty {}